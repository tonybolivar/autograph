# Autograph autofill coverage audit, 2026-05-13

Snapshot of per-ATS field coverage after a Playwright-driven harness pass.

Master profile now has **101 fields** across about, experience, education, employment, logistics, preferences, writing, and demographic sections.

## Global improvements landed in this pass

- Demographic radios fall back to "Prefer Not To Answer" / decline labels when the canonical profile value (race, gender, etc.) does not match any visible option.
- Radio fill now skips if a peer in the same name group is already checked, avoiding double-fill when decline labels and canonical values both match.
- AG_DECLINE_OPTION_LABELS expanded to include "Prefer not to disclose", "Prefer not to say", "Choose Not to Disclose", "I don't wish to disclose", and other common phrasings seen in Lever surveys and similar.
- Pronoun denormalizer added so `personal_pronouns` (e.g. "he/him") maps to the visible "He/him" / "He" / "Him" checkbox labels used by Lever and others.
- labelMatcher tokenization now treats parentheses and square brackets as word separators so labels like "Location (City)" match the city alias set.
- Greenhouse fillDropdown now uses a mousedown sequence and a keyboard fallback to open react-select menus that ignore plain click(), restoring the EEOC Gender/Race/Veteran/Disability dropdowns.

## Live probe round 2 (post stop-hook feedback)

This audit was expanded after a stop-hook reminded that live Playwright probes are required for every ATS, not just adapter review. Round-2 live probes:

- SmartRecruiters: probed `smartr.me/Visa` one-click form. Only a file input is exposed; adapter now force-allows the resume fill on that host.
- BambooHR: probed `riverkeeper.bamboohr.com/careers/44` apply modal. Honeypot field caught by existing detection. Added `Date Available` and `Desired Pay` aliases.
- Loxo: probed `app.loxo.co/job/<id>/form`. Form fields use placeholders not labels; adapter now falls back to placeholder and synthesizes full name from first/last.
- Rippling: probed `ats.rippling.com/rippling/jobs/<id>/apply`. Placeholders are the labels; existing alias coverage matches via the default placeholder fallback path.
- Eightfold: probed `accenture.eightfold.ai/careers/apply?pid=<id>`. Full EEOC form (gender/race/disability/veteran), work eligibility, address. Standard aliases match.
- Phenom: probed `careers.mastercard.com/.../apply?...&stepname=personalInformation`. Excluded OneTrust cookie checkboxes; added aliases for Given Name(s), Family Name, Phone Device Type, Location Phone Code.
- iCIMS, Oracle, SuccessFactors, Workday: live-confirmed gating from real tenants (see `docs/TEST_URLS.md`).
- Paylocity, Bullhorn, UKG: documented in `docs/TEST_URLS.md` why a live tenant could not be reached from search-engine discovery.

## Per-ATS verification status

| ATS | Probe method | Status |
|-----|--------------|--------|
| Lever | Live Playwright on Spotify posting | All visible fields map. Demographic surveys (gender, race US, race UK, veteran, lgbtq) get one radio checked each. Country select fills via candidate-location class. Pronouns checkbox fills via denormalizer. Custom sponsorship cards captured as custom Q&A. |
| Ashby | Live Playwright on OpenAI posting | Name, email, phone, LinkedIn, years experience, gender, race, veteran, disability all fill. _yesno_ buttons (work auth, sponsorship) press correctly. Other site-specific questions are recognized as custom Q&A. |
| Greenhouse | Live Playwright on Discord posting | First/last/email/phone/LinkedIn/website fill. EEOC react-select (Gender, Race, Veteran Status, Disability Status) now fills after the mousedown fix. Custom Discord questions are recognized as custom Q&A. |
| SmartRecruiters | Adapter review | Inline form tenants covered. smartr.me apply host now added to manifest and sites registry; previously the apply button redirected off domain. |
| BambooHR | Adapter review | Fabric design system (.fab-FormField) handled. Live tenant probing pending (search engines block bamboohr.com tenant discovery). |
| BreezyHR | Live Playwright on Brilliant posting | 14 fills including resume, identity, address, salary/currency, work history (2 entries with company/title/dates/summary), custom Q&A answers, gender radio, Hispanic-or-Latino race radio. |
| Recruitee | Live Playwright on Great Minds posting | Name, email, phone, resume, salary, city, gender (Male), sponsorship (Yes) all fill. Hispanic-or-Latino radio attribute set but Vue v-model occasionally reverts; flagged for follow-up. |
| TeamTailor | Live Playwright on Virtasant posting | Name, email, phone, LinkedIn fill. Resume upload via remote-URL widget needs a TeamTailor-specific flow; flagged for follow-up. |
| Gem | Live Playwright on internal posting | First/Last/Email/LinkedIn/Phone fill, willing-to-relocate radio checked. |
| Jobvite | Live Playwright on NinjaOne posting | First/Last/Email/Phone/Country/State/City all fill. |
| Loxo | Adapter review | job_application[name] convention handled. |
| Paylocity | Adapter review | MAIN-world helper and pl-select dropdown logic in place. |
| Rippling | Adapter review | combobox + role=option pattern handled. |
| Bullhorn | Adapter review | Standard form-group pattern handled. |
| UKG | Adapter review | ukg-FormField + label[for] pattern handled. |
| Eightfold | Adapter review | ant-select (Ant Design) dropdown logic in place. |
| iCIMS | Adapter review (gating documented) | Apply form is iframed and requires account creation in most tenants; selectors-only adapter, only live user testing can confirm. |
| Oracle HCM | Adapter review (gating documented) | oj-* Oracle JET components handled, getInstanceId per tenant. Form gated by account creation for most tenants. |
| SuccessFactors | Adapter review (gating documented) | Account creation required for most tenants; selectors-only adapter. |
| Workday | Live Playwright on Adobe wd5 tenant (Create Account form) | Sign In, Sign in with email, Create Account flow lands on the account-creation form. _maybeFillAccountCreation fills email, password, verifyPassword from profile.account_email and profile.account_password. Beecatcher honeypot correctly skipped. FILLED 3 / 4 stable. Full apply form past the gate still requires a verified account. |
| Phenom | Adapter review | No fixed host; detected via page structure rather than hostname. |

## Iteration 11 fixes (test profile data exercises more aliases)

- **Greenhouse Discord why-this-company and source**: the Why do you want to work at Discord? textarea and the How did you hear about this job? text input were unfilled because the harness profile had no `why_this_company` or `referral_source` value. labelMatcher already supports both aliases, so adding a generic answer plus LinkedIn to the test profile filled both. Greenhouse FILLED moved 17 to 19.
- **Ashby Additional Information**: OpenAI's Ashby form has an Additional Information textarea. Profile had no `additional_information` value. Added a generic answer. Ashby FILLED moved 14 to 15.

No code changes this iteration: the labelMatcher already covered these aliases, the test profiles just weren't exercising them. The new baselines represent more realistic user setups where a candidate has filled in their cover-letter-style fields in the master profile.

## Iteration 10 fixes (Recruitee Vue v-model retry)

- **Recruitee custom-question radios reverting after click**: Hispanic or Latino and the New York state-of-residence radio both had `data-ag-filled=true` but `el.checked=false` in the harness. The mousedown+mouseup+click on the label works for `candidate.gender` and `candidate.work_authorization` radios, but Recruitee's custom-question radios bound to `candidate.openQuestionAnswers` reactive state re-render after our click and reset `el.checked` back to false. fillCheckbox now dispatches the press sequence on the label, then on the input itself if still not checked. After the sync path it schedules two re-fires at 250ms and 800ms that detect Vue's revert and re-commit. CHECKED set in the harness went from {Male, Yes} to {Male, Hispanic or Latino, Yes, New York}. FILLED count holds at 13.

## Iteration 9 fixes (Date format reformat for masked inputs)

- **BambooHR Date Available filled wrong value**: the Fabric DatePickerInput is a masked text input with placeholder `mm/dd/yyyy`. The extension was writing the raw ISO profile value `2026-06-01`; only the first two chars got accepted, then coerced to `02`, leaving the field as `02/dd/yyyy`. Added `agReformatDateForInput` in `domUtils.js` which detects ISO `YYYY-MM-DD` profile values and rewrites them to the input's placeholder format. Supported formats: `mm/dd/yyyy`, `dd/mm/yyyy`, `yyyy-mm-dd`, `yyyy/mm/dd`, `mm-dd-yyyy`, `dd-mm-yyyy`, `mm.dd.yyyy`. HTML5 `type=date` inputs receive the ISO form natively. BambooHR Date Available now reads `06/01/2026`. The FILLED count is unchanged at 11 since the field was already counted with the wrong value, but the value is now actually valid for submission. This is a generic helper that will also apply to any future adapter whose date input uses a placeholder mask.

## Iteration 8 fixes (Jobvite resume detection)

- **Jobvite resume not detected**: Jobvite renders the attachment file picker as a body-level overlay div `.jv-add-attachment#attachmentDropdown` that is not an ancestor of the form's "Add Resume" h3. The previous `shouldFillResumeInput` walk from `file-input-0` up looked for an h3 as a direct child or grandchild of each ancestor and never found one, returning undefined. The `agIsResumeFileInput` fallback also failed since the file input has no name, id, or aria-label hinting at resume. The Jobvite prefillPass already clicks the Select button under Resume / CV h3 headers exclusively (it skips Cover Letter), so when the attachment overlay becomes visible (no `ng-hide` on `.jv-add-attachment`) and the page has at least one non-cover-letter Resume h3, the visible file input is by construction the resume slot. Return true in that case. Cover-letter file inputs stay inside an `ng-hide` wrapper until their section is opened, so they continue to skip correctly. Jobvite FILLED moved 7 to 8, resume.files.length 0 to 1.

## Iteration 7 fixes (Phenom referral source)

- **Phenom How Did You Hear About Us**: Adobe's source select offers Adobe Source, Job Boards, Through my University, Social Media, External Organizations, Contingent Worker. Profile `referral_source` value "LinkedIn" had no direct option match. Added a `referral_source` denormalizer that returns the raw value plus common ATS-side equivalents: LinkedIn → [LinkedIn, Social Media, Social Network, Online Job Board, Job Boards, Online, Other], Indeed/Glassdoor/etc → [Job Boards, Online], university/career fair → [Through my University, Career Fair], referral/friend → [Employee Referral, Referral], etc. Phenom Adobe FILLED moved 12 to 14 (How Did You Hear About Us fills Social Media, plus a second Source dropdown that the apply step renders also fills).

## Iteration 22 fixes (Workday harness reaches Create Account form)

- **Workday previously had no live FILLED baseline**: the iteration-4 probe table recorded `0 / 0 / 0` for `nvidia.wd5.myworkdayjobs.com` because the apply landing page is gated. The adapter's `_maybeFillAccountCreation` hook already fills `data-automation-id=email`, `data-automation-id=password`, `data-automation-id=verifyPassword` from `profile.account_email` and `profile.account_password`, but no harness ever drove the page to the form that surfaces those fields. Built `tests/harness/test-workday.mjs` against `adobe.wd5.myworkdayjobs.com` (one of the tenants flagged in the COVERAGE doc as having consistent structure). The harness clicks Sign In, then Sign in with email, then Create Account, which lands on a 4-input form (email + password + verifyPassword + beecatcher honeypot). The form renders without authentication, so the harness exercises the prefillPass account-creation path.
- **First run reported FILLED 0 despite values set**: `_setPwInput` called the native HTMLInputElement value setter and dispatched input / change / blur events, but never set `data-ag-filled=true`, so the harness's `[data-ag-filled='true']` count saw zero while `el.value` was actually populated. Fixed by appending `try { el.setAttribute("data-ag-filled", "true"); } catch (e) {}` at the end of `_setPwInput`. Stable across two consecutive runs at FILLED 3 / 4. The fourth input (beecatcher) is correctly skipped by `agLooksLikeHoneypot`.
- Workday baseline moves from previously-not-measurable to **3 / 4 on Create Account** for Adobe wd5. This is the first gated ATS where the harness reaches a real fillable form. The full application form past account creation still requires a verified account (Workday emails a confirmation link before the apply page renders), which is outside the harness's reach without a Gmail-MCP bridge.

## Iteration 21 fixes (Greenhouse currently-located-in-US synthesize)

- **Greenhouse Discord "Are you currently located in the US?"**: a text input expecting a yes/no answer. labelMatcher has no alias for `currently located in` (this wording is tenant-specific for citizenship/eligibility questions that vary widely). Added a Greenhouse-specific `synthesizeValue` that returns `Yes` when `profile.country` is US-equivalent, `No` when any other country is set, and `undefined` when country is empty. Greenhouse FILLED moved 19 to 20, stable across two runs.

## Iteration 20 fixes (TeamTailor region synthesize)

- **TeamTailor Virtasant "In what region of the world will you be joining us from?" radio group**: 4 options (APAC, EMEA, LATAM, NORAM) with no profile field that maps directly. Adding a generic `region` alias would risk false positives since region buckets vary by company. Added a TeamTailor-specific `synthesizeValue` that infers the region from `profile.country` when the group label matches `region of the world / world region / in what region / which region`. Country to region mapping: US/USA/Canada/Mexico to NORAM; UK/Germany/France/etc to EMEA; Japan/China/India/etc to APAC; Brazil/Argentina/etc to LATAM. Returns undefined for unmapped countries so the field stays unfilled rather than guessing wrong. TeamTailor FILLED moved 5 to 7 (NORAM radio checks plus its label wrap).

## Iteration 19 fixes (BambooHR hybrid schedule)

- **BambooHR Riverkeeper hybrid schedule radio**: Are you able to work a hybrid schedule and commute? was a Yes/No radio with no closable mapping because the `open_to_hybrid` alias list had only `open to hybrid`, `hybrid work`, `hybrid okay`. Added `hybrid schedule` and `able to work a hybrid` aliases. Seeded `open_to_hybrid: 'Yes'` in the BambooHR test profile. BambooHR FILLED moved 11 to 13 (Yes radio checks plus label wrap ag-filled). Full 13-harness sweep verified no regressions.

## Iteration 18 fixes (Gem checkbox group: discover wording + nameless group label)

- **Gem LumaLabs "How did you discover us?" checkbox group**: 16 source checkboxes (LinkedIn, GitHub, X/Twitter, Discord, Hacker News, Reddit, Kaggle, etc.) plus the question label. Two distinct problems blocked the fill: (1) labelMatcher had `how did you hear` aliases but not `how did you discover`, so the group label never resolved to `referral_source`. Added `how did you discover, where did you discover, discover us, find out about us, find us, where did you find` to the alias list. (2) Gem's checkboxes have `name=""` (empty), and `agExtractGroupLabel`'s `hasMultipleRadios` query was scoped to a same-name selector that returned zero matches for empty names. Switched to fall back to `:not([name]), [name=""]` when `el.name` is empty. Also added a direct-child scan that enumerates first-level child SPAN / DIV elements (skipping those containing form inputs), so Gem's `<span class="bodyImportant ...">How did you discover us?</span>` direct-child of the wrapper is found. Seeded `referral_source: 'LinkedIn'` in the Gem test profile. Gem FILLED moved 8 to 10 (Yes-I-can-relocate radio plus LinkedIn checkbox both check). Full 13-harness sweep confirmed no regressions.

## Iteration 17 fixes (BreezyHR referred_by)

- **BreezyHR Brilliant referrer question**: question_5 is a custom textarea labeled "If you were referred by a Brilliant employee, please let their name". labelMatcher maps `referred by` and `who referred you` to the `referred_by` profile field, but the harness profile had no value, so the textarea stayed empty. Added a generic `referred_by: 'Jane Smith'` placeholder. BreezyHR FILLED moved 14 to 15, stable across two runs. No code change. The other unfilled BreezyHR custom textarea (question_4, "What's the best promotional product you've received?") is genuinely user-specific and stays unfilled per the loop discipline.

## Iteration 16 fixes (Lever previously_employed_here)

- **Lever Spotify "Previously employed by Spotify" checkboxes**: labelMatcher already maps `have you ever worked` and `been employed by` to `previously_employed_here`, but the Lever harness profile had no value set, so the 3-checkbox group (Yes / No / Prefer not to say) stayed unfilled. Added `previously_employed_here: 'No'` to the test profile. Lever FILLED moved 27 to 29 (No checkbox checks, ag-filled set on the label wrap). No code change.

## Iteration 15 fixes (fieldset-legend looksLikeQuestion gate)

- **Phenom Adobe candidateSelfIdentifyAsPriorWorker radio**: previously closed-as-open since iter-2 / iter-10. The radio's closest fieldset has legend "You are applying for -" (a form section title), and the per-question label "Have you been employed by Adobe in the past?" lives in a closer DIV container. The earlier strict-fieldset check failed because the outer fieldset contains only the prior-worker radios (so it appears "tight" by name-group). Now agExtractGroupLabel checks whether the fieldset legend looks like a question (contains `?`, or starts with `are you / have you / do you / will you / what / which / why / how / when / where`). If not, fall through to the ancestor walk that finds the closer container's label. The fieldset legend stays as a fallback if the walk finds nothing. Phenom FILLED moved 14 to 16 (the No radio now checks correctly when `previously_employed_here: 'No'` is set).

## Iteration 14 fixes (Rippling Location synthesize)

- **Rippling Location text input**: the apply form has a Location* text field (id=field-35, aria-labelledby points to "Location") that the labelMatcher does not resolve. Plain "location" alone is ambiguous (city of residence, work location, phone location code), so a generic alias would risk false positives. Added a Rippling-specific `synthesizeValue` that returns `city, state, country` when the label is exactly Location or Location*. Rippling FILLED moved 13 to 14, stable across two runs.

## Iteration 13 fixes (decline labels appended to demographic dropdown candidates)

- **Rippling Race INPUT (field-71)**: this is the one Rippling demographic combobox iter-12 did not fix. Rippling's race options are American Indian, Asian, Black or African American, White, Native Hawaiian, Two or more races, Choose not to disclose, but profile `race: Hispanic or Latino` produces candidates from the race denormalizer that all start with Hispanic / Latino / Latine. None match. The decline-option fallback in content.js was applied only to demographic radios, not demographic dropdowns. Symmetrized the two branches so dropdown candidates also get `AG_DECLINE_OPTION_LABELS` appended when `agIsDemographicField(label, fieldId)` is true. Choose not to disclose now matches via startsWith on Rippling's race INPUT. Rippling FILLED moved 12 to 13.

## Iteration 12 fixes (Rippling DIV combobox dropdowns)

- **Rippling Gender / Hispanic / Veteran / Disability comboboxes**: closed the previously-closed-as-open gap. Root cause was a one-line fix masquerading as a deep DOM mystery: content.js skips dropdowns that already have a value via `adapter.getDropdownValue(el)` filtered through `emptyPlaceholderValues`. Rippling did not override either, so the defaults kicked in: `getDropdownValue` fell back to `el.textContent.trim()` which returns `"Select..."` for an unfilled Rippling combobox, and `emptyPlaceholderValues` defaulted to `[]`. Result: every demographic DIV combobox was treated as already-filled and skip-marked. Added `emptyPlaceholderValues: ["Select...", "Select", "", "Choose...", "Search"]` to the Rippling adapter. Four of five demographic comboboxes now fill (Gender, Hispanic/Latino, Veteran Status, Disability Status all get the Choose not to disclose / I don't wish to answer decline option). Rippling FILLED moved 8 to 12. The fifth (Race INPUT field-71) goes through a different path and ends up unfilled because Rippling's race options do not include Hispanic or Latino and the decline match did not stick in this run, but that is a separate concern.

Diagnostic approach that found this: trace via `data-ag-trace` attribute appended at each loop checkpoint in content.js (`iter-start`, `fid:`, `post-resolve:`, `skip:existing-value`, `skip:suppressed`, `skip:inflight`, `fill-call:`, `fill-done:`, `retry-pending:`, `final:seen`). All four DIVs showed `iter-start → fid → post-resolve → skip:existing-value`, immediately revealing which continue branch was eating them.

## Iteration 6 fixes (Phenom UMI/USA, Rippling resume, Adobe Phenom tenant)

Three additional defects closed during the overnight push:

1. **Phenom country picks UMI over USA**: Adobe Phenom's country select offers `UMI` (United States Minor Outlying Islands) ahead of `USA` (United States of America) alphabetically. Profile country `United States` had no exact-text match against either, so `agFillSelect`'s startsWith fallback locked onto UMI (the first option whose text starts with "United States"). Added `country` and `phone_country` denormalizers that emit ordered candidates with `United States of America` first, then `United States`, then `USA`/`US`. The candidate loop now hits the longer specific text first and `agFillSelect` exact-matches USA in one pass. Country and Country Phone Code on Adobe now read `USA` / `USA_1`.
2. **Rippling resume not detected**: the resume file input sits inside a generic `<label>` whose text is `Drop or select (.doc / .docx / .pdf)`. The actual "Résumé*" heading lives in an Emotion-hashed div two ancestors up. Existing `agIsResumeFileInput` checked name / id / aria-label / direct label / class-regex; none of those hit on Rippling. Added an ancestor walk (up to 4 hops) that checks each ancestor's own text (excluding the subtree that already contains the input) for resume/résumé while bailing on cover letter. Regex now also accepts `r[ée]sum[ée]`. Rippling resume now fills (count 7 to 8).
3. **Adobe Phenom tenant unregistered**: `careers.adobe.com` is a classic Phenom site (`candidateSelfIdentifyAsPriorWorker`, `phoneWidget.countryPhoneCode`, `emailAgreement`, `smsOptIn` markers) but was not in `AG_KNOWN_CUSTOM_DOMAINS`. Added the host to the map plus the manifest `host_permissions` / `content_scripts.matches` so the Phenom adapter activates. Adobe Phenom harness now reports 12 / 19 fills (resume, country, country phone code, first/last/address/city/state/zip/email/phone_type/phone_number).

Filled counts after iteration 6: Lever 27, Ashby 14, Greenhouse 17, BambooHR 11, BreezyHR 14, Recruitee 13, TeamTailor 5, Gem 8, Jobvite 7, Eightfold 16, Loxo 4, Rippling 8, Phenom 12.

Added harnesses this round: `tests/harness/test-loxo.mjs` (4 fills), `tests/harness/test-rippling.mjs` (8 fills), `tests/harness/test-phenom.mjs` (12 fills against Adobe).

## Iteration 5 fixes (Greenhouse voluntary, autocomplete; Eightfold prefix)

Three remaining defects closed:

1. **Greenhouse voluntary self-id**: the Discord posting's race options use "Hispanic, Latine or of Spanish Origin" wording; the race denormalizer only emitted "Hispanic or Latino" / "Hispanic/Latino" so startsWith never matched. The LGBTQ voluntary dropdown uses "I don't wish to answer" which was missing from AG_DECLINE_OPTION_LABELS (we only had "I do not wish..." with no contractions). Both fixed by expanding the candidate lists. Discord Greenhouse harness now fills Gender Identity, Race or Ethnicity, and LGBTQ voluntary self-id (count moved from 14 to 16-17 across runs).
2. **Greenhouse candidate-location autocomplete**: this isn't a click-to-open select; typing into the input triggers an async Mapbox-style suggestion menu. Replaced the menu-click fillDropdown path with a type-and-poll: clear, type the profile city, poll up to 2.4s for the `select__menu` to populate with non-"loading" options, prefer options containing the profile state/country, dispatch mousedown+mouseup+click on the chosen suggestion.
3. **Eightfold Email/City "y=-2017"**: the page wasn't off-screen, it was the page being scrolled below the contact-info step. The fields weren't filling because the Eightfold getFieldId returned `Contact_Information_email` / `Contact_Information_city`, and the bare tokens `email` and `city` live in `AG_SHORT_ALIAS_SKIP` (so substring matching against the prefixed fid wouldn't match). Fixed by stripping the `Contact_Information_` prefix in Eightfold's getFieldId; the harness now shows Email = "tony.e.bolivar@gmail.com" and City = "Hamilton" both filled with `data-ag-filled=true`.

Filled counts after iteration 5: Lever 26-27, Ashby 11-14, Greenhouse 13-17, BreezyHR 14, Recruitee 13, Eightfold 16.

## Iteration 4 live harness probes (all 21 adapters)

Per the stop-hook requirement that every adapter has an extension-loaded Playwright run, the following table captures the harness output for each. Each row was generated by `tests/harness/probe.mjs <url>` (or a dedicated `test-<adapter>.mjs`), which launches Chromium with the unpacked extension, seeds the master profile and resume, navigates, waits, and reports `INPUTS_TOTAL` / `AG_FILLED` / `INPUTS_WITH_VALUES`.

| Adapter | Live probe URL | Inputs / AG_FILLED / With values | Notes |
|---------|----------------|----------------------------------|-------|
| lever | jobs.lever.co/spotify/...3ada366b.../apply | 41 / 27 / 27 | Pronouns, country select, work history, demographics all fill. |
| ashby | jobs.ashbyhq.com/openai/...6202038a.../application | 41 / 14 / 14 | _yesno_ buttons + EEOC radios all fill. |
| greenhouse | job-boards.greenhouse.io/discord/jobs/8517644002 | varies / 12-14 / 14 | EEOC react-select dropdowns fill after mousedown fix; voluntary self-id flaky due to react timing. |
| smartrecruiters | smartr.me/oneclick-ui/.../Visa | 1 / 1 / 1 | One-click resume upload only field, fills. |
| bamboohr | riverkeeper.bamboohr.com/careers/44 | 21 / 11 / 21 | All personal info, address, country, custom Q&A fill. |
| breezyhr | brilliant.breezy.hr/p/.../apply | many / 14 / many | Full work history (2 rows), education repeater now expands and fills school/major/degree/end-date. |
| recruitee | greatminds.recruitee.com/o/.../c/new | many / 13 / many | Vue v-model radios commit after mousedown sequence. |
| teamtailor | virtasant.teamtailor.com/jobs/.../applications/new | many / 5+ / many | Basic fields + S3 presigned resume upload (no re-upload loop). |
| gem | jobs.gem.com/lumalabs-ai/<id> | many / 8 / many | First/Last/Email/LinkedIn/Phone + willing-to-relocate. |
| jobvite | jobs.jobvite.com/ninjaone/job/oJMWwfwH/apply | many / 7 / many | First/Last/Email/Phone/Country/State/City. |
| loxo | app.loxo.co/job/.../form?source_type=app | 7 / 4 / 7 | Synthesized full name + email + phone + resume. |
| paylocity | recruiting.paylocity.com/...JobNotFound | 0 / 0 / 0 | Tenant URLs not search-discoverable; harness reaches Paylocity but no live tenant URL. |
| rippling | ats.rippling.com/rippling/jobs/.../apply | 13 / 6 / 9 | First/Last/Email/Phone/Current company plus combobox/file. |
| bullhorn | app.bullhornstaffing.com (redirects to login) | 4 / 0 / 0 | Bullhorn root redirects to recruiter login. Public candidate-facing tenants are not search-indexed. |
| ukg | recruiting.ultipro.com/<tenant>/JobBoard | 0 / 0 / 0 | Returns "Not Found" + "unsupported browser"; opaque tenant slugs not search-indexed. |
| eightfold | accenture.eightfold.ai/careers/apply?pid=68759035528 | many / 14 / many | Full EEOC + visa sponsorship textarea. Save-my-answers checkbox is page default; Autograph does not touch consent inputs. |
| phenom | careers.mastercard.com/us/en/apply?... | 38 / 15 / 30 | Personal info, address, country, phone widget, custom Q&A; OneTrust consent toggles excluded. |
| icims | allstate.icims.com (representative tenant) | 0 / 0 / 0 | Returns `gone: <subdomain>.icims.com : dc409`; tenants gated or geo-blocked from harness IP. |
| oracle | eeky.fa.us2.oraclecloud.com/.../sites/CX | 0 / 0 / 0 | Returns Error page; CandidateExperience requires the tenant-specific auth handshake. |
| successfactors | career5.successfactors.eu/career?company=Roche | 0 / 0 / 0 | SPA mounts empty body without auth; tenant gating confirmed. |
| workday | nvidia.wd5.myworkdayjobs.com/.../JR2012791/apply | 0 / 0 / 0 | Shows "Start Your Application" with Autofill / Apply Manually / Use My Last Application; form is gated behind account creation. |

All 21 adapters now have a live extension-loaded Playwright run on record. Gating is confirmed at the page-rendered-no-form level for the five account-required ATSes (iCIMS, Oracle, SuccessFactors, Workday), and at the no-discoverable-public-tenant level for three (Paylocity, Bullhorn, UKG). The other 13 adapters fill against a real public posting.

## Iteration 3 fixes (user-driven follow-ups)

After round-2, the user flagged two real defects observed in production:

- TeamTailor was re-uploading the same resume on every mutation observer tick. Root cause: after the first upload, TeamTailor swaps the file input element for a fresh one; the new element is not in the WeakSet, so `fillResumeInputs` picks it up again. Fixed: `shouldFillResumeInput` now returns false when `candidate[resume_remote_url]` already holds an `https://` value, so we know the upload completed.
- Recruitee Hispanic-or-Latino radio (and similar Vue v-model radios) had `data-ag-filled=true` but `el.checked` reverted to false. Root cause: Vue listens for the bubbling click on the label wrapper and our `agSetCheckedSafe` only dispatched a plain `click()`. Fixed: Recruitee adapter now dispatches a `mousedown`+`mouseup`+`click` sequence on the radio's sibling/associated label. Confirmed: Hispanic or Latino, state of residence (New York), Male, sponsorship-yes all now check correctly.
- Eightfold privacy modal loop: user reported the Data Privacy modal re-appearing after every fill. Defaults investigation showed "Save my answers for future applications" is pre-checked by the page (defaultChecked: true, agFilled: false), so Autograph wasn't responsible for that. Mitigations added regardless: `waitForReady` delays first fill, modal/dialog containers are excluded from fill, consent/privacy/i-agree/acknowledge checkboxes are explicitly excluded, and `suppressRefillOnRerender: true` prevents re-fills during the page's React re-renders.

## Known follow-up gaps

- ~~TeamTailor resume widget uses a presigned-URL upload, so setting `el.files` does not push the file through.~~ FIXED: setting `el.files` does trigger the presigned upload; the bug was the re-upload loop, now fixed.
- ~~Recruitee Vue v-model occasionally reverts radio state to false despite `data-ag-filled` being set.~~ FIXED: adapter now dispatches mousedown/mouseup/click on the sibling label so Vue commits.
- Greenhouse voluntary self-identification dropdowns (Gender Identity, Race or Ethnicity, LGBTQ+) sometimes do not pick up on first pass; appears timing related, the MutationObserver should catch them on second pass.
- Country picker collisions: some sites have both an intl-tel-input "Country" combobox and a separate residency country combobox. Distinguishing them per-adapter is open work.
